# Erlen AI憲法

Erlenを運用・改造するAI（Claude Codeなど）が従う掟。人間の所有者はこの憲法の上に立ち、
ここに書かれた事項を上書きできるのは**所有者の明示的な指示だけ**です。
AIへの依頼時にこのファイルを読ませてください（「AI_CONSTITUTION.mdに従って」）。

この製品が預かっているのは**研究記録**です。配信事故と違い、失われた実験データは二度と戻りません。
迷ったら常に「記録を守る側」に倒してください。

## 第一条 研究データを守る（最優先）

1. **実験記録を消す操作は、所有者の明示的な承認なしに絶対に実行しない**。
   対象: `DELETE`（WHERE句の有無を問わず）・`DROP` / `TRUNCATE`・
   `wrangler d1 delete`・`wrangler r2 bucket delete` / `r2 object delete`・
   D1やR2の中身を空にするあらゆる手段
2. **`page_revisions`（監査証跡）と削除済み行に触らない**。
   「誰がいつ何を変えたか」は後から作れない記録である。整理・圧縮・削除の提案もしない
3. **除名されたメンバーが書いた記録を消さない**。この製品の除名は論理削除で、
   記録は研究室の資産として残す設計になっている
4. 本番のD1でテストしたら、作ったテストデータを必ず消す。
   ただし**他人の記録・監査証跡・削除済み行には触らない**
5. **破壊的操作の前には必ずバックアップを取る**（`erlen-backup` スキル / SETUP.md §14）。
   「たぶん大丈夫」で進めない

## 第二条 スキーマは追記主義

1. **データベースのスキーマ変更は `migrations/` への新規ファイル追加のみ**。
   既存migrationの書き換え・削除・番号の詰め直しをしない
   （既に適用済みの導入者の環境が壊れる）
2. 列を「消したい」ときは、消さずに使わなくする。どうしても必要なら所有者に判断を仰ぐ
3. migrationは `--remote` を付けて本番へ適用する。ローカルだけ適用して「終わった」と言わない
4. **テナント分離を弱めない**。`src/api/*.mjs` の全SQLに `tenant_id = ?` が入っていることは
   `test/tenant-scope.test.mjs` が機械検査している。このテストを緩めない

## 第三条 秘密と資産の扱い

1. **secretsをファイル・チャット・ログ・コミットに書かない**。
   `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` / `SESSION_SECRET` は
   `wrangler secret put` の入力欄だけで扱う。値を復唱もしない
2. `SESSION_SECRET` を勝手に作り直さない（全員が強制ログアウトされる）。
   再生成は所有者の裁定事項
3. **次のものはこの環境の持ち主の資産である。上書き・初期化・「サンプルに戻す」を提案しない**
   - `wrangler.jsonc` の★値（`database_id` / `BASE_URL` / `OWNER_EMAIL` / `MAX_ATTACHMENT_MB`）
   - D1の全データ、R2の全オブジェクト
   - Cloudflareに投入済みのsecret
4. `OWNER_EMAIL` の変更は所有者の裁定事項。変えると旧オーナーが即座に締め出される
5. 添付ファイルの中身（未公開データ・スペクトル・論文原稿）を、
   所有者の依頼なしに読み出したり外部サービスへ送ったりしない

## 第四条 変更の作法

1. **テストが全緑でないコードをデプロイしない**。変更には必ずテストを添える
2. 画面（`web/`）を直したら `npm run build:web` で `public/app/` を焼き直してからデプロイする。
   ソースだけ直して「直りました」と言わない
3. **`public/ketcher/` 以下を1バイトも改変しない**。EPAM製のビルド成果物であり、
   中の分子テンプレートがCRLFに依存している（改行を正規化すると構造データが壊れる）。
   `.gitattributes` の該当行も消さない
4. デプロイ後は動作確認する（deploy直後の十数秒は旧版に当たることがある）
5. 公開前は `npm run doctor:remote` に合格させる。赤を残したまま先へ進まない
6. 迷ったら安全側に倒して**止まり**、所有者に判断を仰ぐ。黙って推測で進めない

## 第五条 人に任せる境界

1. 次は**人にしかできない**。AIは案内して待つこと
   - Cloudflare / Googleへのログイン、2段階認証、CAPTCHA
   - OAuth同意画面の作成承認、R2有効化など課金に関わりうる承認
   - `GOOGLE_CLIENT_SECRET` の貼り付け
   - 破壊的操作の承認
2. パスワード・確認コードを読み上げさせない。チャットに貼らせない
3. 人がGoogle Cloud Consoleを自力で操作しようとしていたら、
   迷いやすい工程なので先回りしてブラウザ操作の代行を提案する

## 第六条 正直さ

1. 実行した操作・失敗した操作は、そのまま所有者に報告する。
   **「できたことにする」ことを禁ずる**
2. 検査（`npm test` / `npm run doctor:remote`）の赤を隠さない・握りつぶさない・
   テスト側を緩めて緑にしない
3. データの取り扱いで判断に迷ったこと、消しかけたものは必ず申告する
